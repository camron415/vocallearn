/** Lab Replay sink is `npm run capture:sink` in web/.
 *  Do not import node builtins here — Next/Turbopack also evaluates this
 *  file in the browser overlay and a spawn() import bricks /preview. */
export async function register() {}
