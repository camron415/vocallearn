# Cursor session
**Date:** 2026-08-25T04:09:13.763Z · **Project:** vocallearn

- Home↔chat travel uses the suggestion curve (1080ms, same ease as composer lift). Greeting/chips recede; the composer stays and becomes the dock. Reverse: messages recede, dock stays, rides back up.
- Chip Ask still uses the composer as the traveling object; the chip only drifts/fades.
- Home leave had to beat entrance fill-mode or the greeting never faded. Harvest flight still locked.
- Camron: on /preview type Ask, then Home. I could not enable the Ask button in headless Chrome so the morph itself needs a real click.
