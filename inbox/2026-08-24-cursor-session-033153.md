# Cursor session
**Date:** 2026-08-24T03:31:53.799Z · **Project:** vocallearn

- Home Keep candy was resetting to gray on Safari refresh because CSS used rgb(var(--bead) / alpha), which Safari drops on a cold parse, plus .capsule__fill { fill: transparent } later in the sheet, plus backdrop-filter + clip-path. Not mixer defaults.
- Fix: paint Keep fills as inline hex on the chip (glass + SVG fill), hex CSS vars, no backdrop on Keep glass, mixer saved to localStorage as well as sessionStorage so closing the tab does not wipe Match/Citrus/Lift.
- Scatter default is 22. Gutters pushed a bit farther down/out. Asks stay gray until Keep.
- Harvest in-chat flight stays locked. Lab-only deploys.
- Hard-refresh /preview: facts stay citrus (Nile/Egypt/Rome/476/Colosseum/1776). Mixer persists.
